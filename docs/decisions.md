# Decision inbox

New load-bearing decisions land here first, as lightweight ADRs (context, decision, why, rejected alternatives). Once agreed with Nicolai, each entry is **consolidated natively into the owning living doc** — the relevant PRD, `architecture.md`, or `vision.md` — and purged from this file. The cycle then repeats.

The living docs are the truth; this file is only the staging area. The full historical ADR log (D1–D32 and their consolidation) lives in git history.

*Inbox is currently empty. Last consolidation: 2026-07-14.*

---

## Number allocation — **next free is D58**

Living docs carry decisions as **prose, never as numbers** (grep confirms: zero D-refs across all of `docs/`). D-numbers exist for two purposes only: **code comments** and **git history**. So this ledger is the one place that records which numbers are spent. Check it before allocating.

**D33–D38 allocated 2026-07-14** for the task file projection (slices 1 + 2). They were originally written in the two plans as **plan-local D1–D15**, which collided head-on with the global registry — code was citing plan-local `D8`/`D9`/`D14` in the same files as global `D19`, and a reader grepping history for `D9` landed on *"chat history: native `--resume`"*. Renumbered globally 2026-07-14 with Nicolai; the plans keep their local numbering as frozen artifacts.

| Global | Was (plan-local) | Decision | Cited in | Consolidated into |
|---|---|---|---|---|
| **D33** | slice-2 D8 | **The `version` token leaves the frontmatter.** Carried out-of-band in the desktop's `ProjectionStore`; git ingress needs no token (a commit carries its own base blob). In the file, every reminder fire would rewrite — and, once mirrored, *commit* — a task file to change one integer. `parseTaskFile` still accepts a `version:` key so old files stay readable; nothing writes one. | `shared/task-file.ts`, `desktop/task-projector.ts` | `prd/tasks.md`, `prd/server-data.md`, `architecture.md`, `glossary.md` |
| **D34** | slice-2 D9 | **Git ingress diffs base-blob vs head-blob — no version guard.** The commit *is* its own diff base, so the per-field patch is exact; a version check would only reject a legitimate remote edit whose commit predates an unrelated local change. Same rule notes already ingest under. `tasks.update`'s `version` stays optional; git never sends one. | `server/git/task-ingest.ts`, `server/tasks/mutations.ts` | `prd/tasks.md`, `prd/server-data.md` |
| **D35** | slice-2 D10 | **Exactly one place a task changes: `src/tasks/mutations.ts`**, called by both the tRPC router and the git ingester. `finishMutation` is where `recomputeReminder` + `bus.emitTasks` + `wakeEvaluator` live — a direct table write fires no reminder and pushes no SSE, so no desktop rewrites the file. | *(no comment cites it; enforced structurally)* | `prd/server-data.md`, `prd/tasks.md`, `architecture.md` |
| **D36** | slice-2 D12 | **Presence is a sibling `presence` frame on the existing per-vault SSE connection** — not a `TasksEvent` variant, not a new relay room. `TaskProjector.applyTasksEvent` reads that union as `if (upserted) … else remove(taskId)`, so a third variant falls into the `else` and **deletes the task's file**. | *(the trap is documented at the consumer)* | `prd/tasks.md`, `architecture.md`, `glossary.md`, drawer spec |
| **D37** | slice-2 D14 | **A user and their agent are one identity for presence.** No `actor` field: the agent runs on the user's token because the user set it going, so "Nicolai is editing this task" is true when Nicolai's Claude is editing it. Distinct from the drawer's "Claude is editing…", which tells *you* what *your own* agent is doing. | `server/bus.ts` | `prd/tasks.md`, `prd/agent.md`, `glossary.md` |
| **D38** | slice-2 D15 | **A deleted folder unfiles its tasks (`ON DELETE SET NULL`); there is no folder tombstone.** The un-cascaded FK made Postgres *refuse* to delete any folder a task pointed at — folders were silently undeletable and the PRD's tombstone was unreachable code. A task may exist without an area; that is what the "(no area)" lane *is*. The raw-id fallback in `serializeTaskFile` survives as a **stale-resolver** guard, not a tombstone. | migration `0004`, `server/db/schema.ts` | `prd/tasks.md`, `prd/server-data.md` |

Slice-2 plan-local **D11** (task files win a path collision with a doc at `tasks/**.md`) and **D13** (presence is fire-and-forget, server-stateless) were consolidated as prose without needing a global number — nothing in code cites them.

**D39–D40 allocated 2026-07-14** for the offline reconcile.

| Global | Decision | Cited in | Consolidated into |
|---|---|---|---|
| **D39** | **The reconcile path sends its patch with no version guard.** It diffs against the `ProjectionStore` — the true base, the exact bytes the writer edited — so the per-field patch is exact and a version check protects nothing, while destroying legitimate edits: `version` bumps on every mutation *including a reminder firing server-side with no human involved*, so going offline, editing `title`, and having a reminder fire would throw the edit away. Untouched fields are never sent, so a teammate's concurrent change survives. Same argument as D34 for git ingress, and it makes the two inbound paths agree. *Live* watcher writes stay guarded. | `desktop/task-projector.ts` | `prd/tasks.md` |
| **D40** | **A failed offline delete is retried on reconnect; a delete is never *inferred* from a missing file.** An `rm` while disconnected fails its mutation, so the record outlives the file — and reconcile's `write()` would re-materialize it, silently undoing the `rm`. Attempted-but-failed deletes are tracked **in memory** and retried. Dropped on restart on purpose: there the older, deliberate rule takes over — a file missing at `start()` is re-materialized, not read as a delete. Inferring deletes from absent files would let a half-synced working copy destroy records. | `desktop/task-projector.ts` | `prd/tasks.md` |

**D41–D42 allocated 2026-07-14** for the board.

| Global | Decision | Cited in | Consolidated into |
|---|---|---|---|
| **D41** | **`overdue` and `p1`/`p2`/`p3` are *virtual* labels, computed at render — never stored.** They render as chips beside real tags and filter identically, so the board reads like a labelled issue list. But nothing writes them. **Why not store them:** something would have to write `overdue` onto a task the moment it tipped over at midnight, and every such write bumps `version` → rewrites the task file → **with the git mirror on, the bot commits it**. A hundred tasks going overdue is a hundred commits on an idle vault — the exact failure D33 exists to prevent. It would also make `tags` half machine-owned, so an agent deleting `overdue` would have it silently re-added. `priority` and `due` stay real fields; the labels are a *rendering* of them. | `shared/labels.ts` | `prd/tasks.md` §Board UX |
| **D42** | **A board cell is `(column, lane)`, and one drop is one patch.** Dropping a card sets `status` **and** `area` in a single `tasks.update`. **Why:** the two drag axes as separate mutations would make a diagonal drag two writes, two pushes, two file rewrites and (mirrored) two commits — and a half-failed pair leaves the card somewhere nobody dropped it. | `renderer/state/tasks.ts`, `BoardView.tsx` | `prd/tasks.md` §Board UX |

**D43 allocated 2026-07-14** for the board's slice 2.

| Global | Decision | Cited in | Consolidated into |
|---|---|---|---|
| **D43** | **The lane-depth control is deferred — because a collapsed lane has no `area` to drop into.** The PRD offered a depth control collapsing `projects/a` and `projects/b` into one `projects` lane. Grouping is trivial; **dropping is not**: a merged lane cannot say *which* folder a dropped card lands in, so the horizontal drag axis — a real write, and the whole reason `area` is settable rather than derived — becomes undefined exactly when the control is on. Shipping it would mean a silent guess or a lane that accepts no drops. Deferred until there is an answer; the lane machinery does not design it out. | *(structural — nothing cites it)* | `prd/tasks.md` §Board UX |

**D49 allocated 2026-07-16** for membership.

| Global | Decision | Cited in | Consolidated into |
|---|---|---|---|
| **D49** | **A personal vault is blocked from membership mutations by a *type*, not a habit: `sharedVaultProcedure` + a branded `SharedVaultId`.** **The bug:** `invite` and `transferOwnership` had **no vault-kind guard** — you could invite a stranger into your personal vault, or hand it away. auth-identity FR-14 forbids both, and it is not cosmetic: daily notes rest on a personal vault having exactly one owner and therefore exactly one clock (**D44/D45**), and a second member makes "today" ambiguous — the precise problem personal-only scoping exists to sidestep. `setRole`/`remove` escaped only because `assertNotLastOwner` happens to catch them, and `leave` because owners cannot leave — reporting *"owner must transfer ownership first"*, implying you could leave your personal vault if you transferred it. Safe by accident, and lying about why. **Why a brand, not an `if`:** a runtime check per op is a guard you can forget, and this one was forgotten twice. `membership/service.ts` takes `SharedVaultId`, which only the middleware produces, so an op that skips the kind check **does not compile** (verified: `Type 'string' is not assignable to type 'SharedVaultId'`). The middleware is the hard block; the type is what stops the next op being added without it. `list` deliberately stays on `vaultProcedure` — FR-14 disables invite/leave/transfer, not reading your own single membership row. Agreed with Nicolai 2026-07-16. | `server/trpc.ts`, `server/membership/service.ts`, `renderer/state/members.ts` | `prd/auth-identity.md` FR-14 |

**Also settled while building it, and not numbered:** **invite reuses `config.google.workspaceDomain`** — the knob sign-in already enforces via `assertWorkspace` — rather than a second domain setting or a hardcoded `@syv.ai`, so the two can never disagree about who is allowed in (FR-10). **Unset ⇒ unrestricted**, matching `assertWorkspace`: the var is unset in dev by design, and a hardcoded domain would fail every local invite while reading as correct. `assertNotLastOwner` is **not** the personal-vault guard though it has been mistaken for one — it blocks removing or demoting the last owner of *any* vault, a rule that must survive independently of D49.

**D47–D48 allocated 2026-07-16** for reminder delivery.

| Global | Decision | Cited in | Consolidated into |
|---|---|---|---|
| **D47** | **Reminder delivery is a per-`(vault, user)` ledger, and `reminders.fired` becomes `fired_at`.** **Why:** `tick()` stamped the flag and *then* emitted into a bare EventEmitter — with no SSE listener the fire evaporated while the row said it had happened, and `listPending` only returns unfired rows, so nothing could find it again. `tasks.md`'s headline story ("even if my app was closed at the moment") was structurally unsatisfiable, and no amount of client work could fix it. **One column, not a bool beside a timestamp:** they would be redundant (`fired` ≡ `fired_at IS NOT NULL`) and the drift is invisible — set one, forget the other, and catch-up silently skips a fire. **Per member, not per reminder:** a fire concerns everyone in the vault (a task has no assignee), so "delivered" is a fact about a person. **Not `per_user_state`:** it fits the shape but is client-writable, and a client that can move its own watermark can silently disable its own reminders. Catch-up advances to the highest fire it *returned*, never to `now()` (a fire landing between read and write would be stepped over — the same silent-loss class); the advance is `greatest(...)`, so it only moves forward. | `server/reminders/delivery.ts`, `server/events.ts`, `server/db/schema.ts`, `desktop/main/vault/vault-manager.ts` | `prd/tasks.md` §Recurrence & reminders |
| **D48** | ~~**Reminders are delivered for the vault you have open; live cross-vault delivery is deferred.**~~ **CLOSED 2026-07-16 by D50.** The desktop held **one** SSE connection, for the active vault, so a fire elsewhere could not arrive live — the ledger (D47) meant it was never *lost*, it surfaced when you next opened that vault. The fix was named correctly here and was carried out exactly as written: a **user-scoped** event stream replacing the per-vault one, in its own slice, with the ledger untouched because it was already per-user. Reminders now arrive for every vault you are in, whether or not it is on screen; `reminders:open` gained a `vaultId` so the click can switch to it (D52). Verified live: with the app sitting on another vault, a fire in the personal vault reached the notifier. Kept rather than deleted — the row records why the delivery slice stopped where it did, and D50 is the answer to it. | *(structural — nothing cites it)* | `prd/tasks.md` §Recurrence & reminders |

**Also settled while building it, and not numbered:** above the evaluator's coalesce threshold the client raises **one summary** rather than N notifications (six toasts is not six times the information — it is a wall you dismiss unread, which loses all six), and a summary carries **no** `taskId` because it speaks for several tasks, so focusing the window is the whole action. `/subtask` also became **`/todo`** while fixing wayside bugs (`prd/notes-editor.md` FR-9): it inserts `- [ ] ` and nothing else, and the name promised a parent task that cannot exist — the editor only ever opens notes, since a task description is a plain textarea.

**D44–D46 allocated 2026-07-16** for daily notes, slices 1–2.

| Global | Decision | Cited in | Consolidated into |
|---|---|---|---|
| **D44** | **The client passes its local date; the server never computes "today".** `getOrCreateDaily` and `sweepDaily` both take `localDate: 'YYYY-MM-DD'` as an **input**. **Why:** the server cannot know the caller's timezone — a server-side `now()` mints the wrong note for anyone east or west of it, and "prior-day" in the sweep would archive *today's* note for someone past midnight. A personal vault has exactly one owner and therefore exactly one clock; that clock is on the device. It also makes both ops **pure with respect to time**, so tests pass a date instead of injecting a clock. The client's `localIsoDate` deliberately avoids `toISOString()` (UTC — reports the wrong day either side of local midnight); `shared/dates.ts`'s `formatDate` is UTC-based and is for epoch math, not for asking a device what day it is. | `shared/daily-note.ts`, `desktop/state/daily.ts`, `server/daily/service.ts` | `prd/daily-notes.md` |
| **D45** | **Daily notes degrade offline — there is no optimistic local create.** Get-or-create is server-only; offline it fails cleanly and you land on the last-viewed surface. **Why:** the unique `(vault_id, path)` index is the *whole* idempotency story. An offline optimistic create abandons it — two of your devices would each mint a Doc claiming one path with no arbitrator, forcing a same-path merge-or-rebind protocol into existence. That decision belongs to **vaults-collaboration**'s sync/bridge layer, and buying it here would mean designing CRDT-union semantics to serve a convenience. A missing daily note offline is a non-event: you get it on reconnect, and nothing was lost because there was nothing in it yet. Closes `prd/daily-notes.md` OQ1 for v1. Agreed with Nicolai 2026-07-16. | `desktop/state/daily.ts` | `prd/daily-notes.md` |
| **D46** | **The sweep selects daily notes by `kind='daily'`, never by filename shape.** The old Rust sweep read the vault root and regex-matched `^\d{2}-\d{2}-\d{4}\.md$` because, reading a directory, it had nothing else to go on; the server has a typed record. **Why it matters:** filename-matching would let the sweep pick up a **hand-authored** note that merely looks like a date — and the GC, finding it empty, would **delete a note the system never created**. `kind` is written only by `getOrCreateDaily`, so only system-minted notes are swept. `isDailyNoteFilename` survives as a **client-side display predicate only**, never a sweep input. | `server/daily/service.ts`, `shared/daily-note.ts` | `prd/daily-notes.md` §Archiving |

**Also settled while building daily notes, and not numbered:** the **stub GC's backref guard** (delete only a stub with zero `link_index` backrefs) is already PRD prose, but the *reason* is worth restating where the code lives — the old sweep deleted stubs freely because an orphan-rescue dialog healed the dangling refs on next launch, and that machinery is deliberately **not** ported (it served `source_file`, which no longer exists). There is no net, so a referenced-but-empty daily note is kept and archived like any other. Likewise, `getOrCreateDaily` returns the full `DocMeta` rather than the PRD pseudocode's bare `{docId, path}` — a superset, matching `notes.create`, sparing the client a refetch-and-find to open what it just asked for.

**Also settled while building slice 2, and not numbered:** the filter's tag control matches **virtual labels and real tags identically** (a direct consequence of D41 — it is what makes "show me the overdue p1s" a tag query instead of two bespoke controls), and the presence heartbeat is driven by **edits, not focus** (merely opening a task must not light up a teammate's card).

**Also settled, and not a numbered decision because it removes one:** *there is no offline patch queue.* The PRD promised edits "queue as patches"; they do not, because the **working copy is the queue** and the **`ProjectionStore` is the diff base**. Both are already durable, so the patch is reconstructible at any later moment. A second queue would duplicate that state and add a way for the two to disagree.

**D50–D52 allocated 2026-07-16** for the user-scoped event stream.

| Global | Decision | Cited in | Consolidated into |
|---|---|---|---|
| **D50** | **The event stream is scoped to a user, not a vault, and every frame is an envelope: `{ vaultId, event }`.** `GET /events` replaces `GET /events/<vaultId>`. **Why it is not three bugs:** the connection's identity *was* a vaultId — from the URL regex through the auth gate to teardown — so anything about **you** rather than about one vault had nowhere to arrive. That one shape caused all of: a stale file tree, a vault switcher that needed a restart, and undeliverable cross-vault reminders (**D48**, now closed). Auth drops `resolveVaultRole` — there is no vault in the URL to check a role against — and membership becomes a **subscription set** (`listVaultIdsForUser`, the same predicate `vaults.list` runs) rather than an admission check, so **401 is the only rejection left; 403 has nothing to mean**. Authorization is *not* broadened: `vaultProcedure`, `requireDocAccess` and the relay's `onAuthenticate` all still gate per vault. **Why an envelope and not a `vaultId` on the payloads:** none of `DocsEvent`/`TasksEvent`/`PresenceEvent`/`RemindersEvent` carries one, because the key `docs:<vaultId>` always supplied it — and each shape is hand-mirrored in up to three client files, so widening them is the **D36** trap (a consumer reading a union exhaustively-by-accident). Multiplexing is a *transport* concern: the payloads say **what** happened, the envelope says **where**. The vault is a closure variable at subscribe time, so D47's `(vaultId, userId)` watermark needs no demux and no change. **Replace, don't add:** two stream routes is two auth models and a dead one. The three dead tRPC subscriptions (`vaults.watchDocs`, `tasks.watch`, `reminders.subscribe`) were deleted with it — unreachable (no WS link; the IPC link throws on subscriptions) and advertising a transport that now never will exist. Agreed with Nicolai 2026-07-16. | `server/events.ts`, `desktop/main/events/user-stream.ts` | `architecture.md` §5, `prd/tasks.md`, `prd/notes-editor.md` |
| **D51** | **Membership is the re-keying signal: a user-keyed bus channel `user:<userId>`, written from exactly one place, emitted only after the transaction commits.** A user-scoped stream that resolves your vaults at connect is **stale the moment you are invited** — the new vault's frames go to a key nobody is listening on, and the switcher is exactly as broken as before. So the stream subscribes to `user:<id>` and adds/removes the four per-vault listeners live. **This is the only reason D50 fixes the switcher**, and it is mutation-proven: disabling the re-key fails precisely the three tests that name it. `wakeEvaluator` was the sole precedent for an unscoped key; this is the first *user*-scoped one. **`MembershipEvent` carries a `vaultId` — the one channel that must**, because its key names a *user*, so the vault is not in the key and would otherwise be nowhere; the handler lifts it into the envelope so the *wire* stays uniform and the desktop mirrors a bare `{ type }`. (I first specified it without one, reasoning the envelope supplies it; that reasoning does not transfer from the vault-keyed channels, and the error was caught writing the stream's tests — `cdd7a46`.) **One place a membership changes** (the **D35** shape): five writers, none of which announced anything, including `vaults.create` and `provisionPersonalVault`, which write a row without touching `membership/service.ts`. They funnel through `insertMembershipRow`/`deleteMembershipRow`, which return the affected userId **or null when the table did not actually change** — a re-invite hits `onConflictDoNothing`, and a frame saying "you joined" when you were already a member is a frame that lies (the rule `getOrCreateDaily` already follows). **The emit is outside the transaction**, and the row helpers take no `Bus` so nothing inside one can misuse it: the bus is an in-process EventEmitter with no transactional semantics, and a listener that reacts by *reading* your vaults (exactly what the stream does, to re-key) would be on another connection, miss the uncommitted row, and skip the vault it was just told about. That property is deliberately **untested** — no tx here can roll back after the membership insert (it is last in every one), and mutation-testing showed a listener-races-the-commit test passes either way; a test that cannot fail is worse than none, so the guard is the missing parameter and the comment on it. `setRole`/`transferOwnership` emit nothing: they only *update* rows, both parties are members before and after, and nobody's vault list changes. | `server/bus.ts`, `server/membership/service.ts`, `server/events.ts` | `prd/vaults-collaboration.md`, `prd/auth-identity.md` |
| **D52** | **Main filters `docs`/`tasks`/`presence` to the active vault; `reminders` and `membership` are never filtered — so `reminders:open` carries a `vaultId` and switches vault before selecting the task.** The renderer holds exactly one tree and one board, and `mirror`/`projector` are constructed inside `activate` and close over one vault. This is a **safety** property, not tidiness: `applyTasksEvent` reads its union as `if (upserted) … else remove(taskId)`, so a frame from *another* vault falling through would **delete a task's file** — **D36**'s trap reached by a new road. Filtering in main also keeps `tasks:event`/`tasks:presence` bare-payload and unchanged, so the board carried no regression risk from the slice, and the renderer never learns to filter by a vault id it has no use for. **Reminders and membership are the deliberate exception**, and they are the whole point: delivering a reminder for a vault you do *not* have open **is** the closure of D48, and being added to a vault is not about any vault you already have — transporting them user-scoped only to drop them in main would buy nothing. **The consequence shipped with it:** a cross-vault notification's click used to `setView('board'); setSelectedTaskId(taskId)` against the *wrong vault* and silently select nothing. The payload gained a `vaultId` and the handler switches first (jotai bails when unchanged, so it is a no-op in the common case). It deliberately skips `selectVault`'s mid-edit guard — that guard is for an accidental switch from the dropdown; clicking a reminder is an explicit instruction to go to that task. A coalesced summary still carries no `taskId` (it speaks for several tasks) but does carry the vault, so it can still land you in the right place. | `desktop/main/vault/vault-manager.ts`, `desktop/main/reminders/notifier.ts`, `renderer/components/Shell.tsx` | `prd/tasks.md` §Recurrence & reminders |

**Also settled while building it, and not numbered:** the **`stream:resync`** push exists because there is **no resume cursor on the wire** (no `id:`, none parsed) — main's mirror and projector reconcile themselves after a gap, but the tree and the switcher have nothing but a refetch, and a gap is precisely the moment they would go stale on the path this slice exists to fix. **`reminders.catchUp` (per-vault) was deleted** in favour of `catchUpAll`: its only caller was the desktop, which knew its vault because the stream was per-vault; a gap now means *every* vault missed fires. It returns `{ vaultId, event }` — the same envelope — rather than a flat concat, because the notifier needs a vaultId per fire and flattening throws away the only thing that could supply it, reproducing D52's bug on the catch-up path. **The per-activation reminder catch-up is gone**: it existed only because the connection did not outlive the vault (the workaround D48 forced) and dissolves with its cause. `createNoteAtom`'s **refetch is gone** too — `created` upserts by id, so the echoed frame agrees with the optimistic apply instead of racing it; two mechanisms updating the tree is worse than either. **Folder rows still have no channel** (`DocsEvent` carries a `DocMeta`; `ensureAncestorFolders` writes rows silently), so `needsFolderRefetch` notices the rare "first note in a new folder" and refetches rather than inventing a row the server never sent. Finally, the **single-node** note (`bus.ts:1`) gets slightly sharper: a user's whole vault set must now be reachable from the one node holding their connection — unchanged for v1, but a real input to the multi-node question rather than a rounding error.

**D53–D54 allocated 2026-07-16** for snapshot history.

| Global | Decision | Cited in | Consolidated into |
|---|---|---|---|
| **D53** | **`snapshots.preview` ships with the timeline — a blind restore is not a feature.** `list` deliberately does **not** select `state` (a full `Y.Doc` per row; shipping every version's bytes to render a list of labels is a footgun that grows with the doc), and there was no `get`/`preview` — so **nothing in the router could tell you what is in a version.** A timeline built on it alone offers "restore 10:31" versus "restore 09:58" and no way to tell them apart, and for `reason:'interval'` rows — which carry a **null label** — the only distinguishing thing on screen is a timestamp. Picking by timestamp is guessing, and that the guess is recoverable (restore takes a `pre-restore` snapshot first) is not a reason to make someone do it. **Why it is cheap:** `docText(docFromState(snap.state))` is the exact pair `restore` already runs before it writes — preview is restore with the write removed. Read-only, and it does not mount a second editor: a version is prose to read. It repeats `restore`'s check that the snapshot belongs to the doc, which is the **only security-relevant line in it** — `requireDocAccess` gates a docId you *can* read, so without it the bytes could come from one you cannot. (The cross-doc test needed a **positive control** to mean anything: tRPC's caller proxy rejects an *unknown procedure* with `NOT_FOUND` too, so the rejection passed cleanly before `preview` existed. Asserting the same call *succeeds* against the right doc is what makes the rejection mean "wrong doc" instead of "no such procedure".) Agreed with Nicolai 2026-07-16. | `server/routers/snapshots.ts`, `renderer/state/history.ts` | `prd/vaults-collaboration.md` §History |
| **D54** | **Retention stays an open question. The timeline folds automatic snapshots; it deletes nothing.** `prd/vaults-collaboration.md` flags pruning as open and warns against inventing a policy — this does not. **The problem is real regardless:** a doc touched all day mints an interval snapshot every 10 minutes, so a visible timeline is mostly unlabelled rows within a week, and the `pre-agent-write` snapshot the whole feature exists to surface is buried among them. **The fix is display, not deletion:** rows split on `reason === 'interval'` — *milestones* (an agent wrote, a rename rewrote links, a restore ran) render in the timeline; *automatic* rows fold behind a "show all versions" toggle, and restore identically when revealed. **Why not prune:** pruning is data loss, it needs its own D#-decision, and the thing forcing the question is a rendering problem that a rendering fix answers completely. **Why not show everything flat:** honest but useless — it buries the milestone rows in noise, which is the same failure as having no UI at all. Nothing about this closes the retention question; `yjs_snapshots` still holds a full `Y.Doc` per row and still grows unbounded. **Splits on `reason`, not on a missing `label`:** every writer except `interval` happens to set a label today, so keying on that would be a coincidence rather than a rule. Agreed with Nicolai 2026-07-16. | `renderer/state/history.ts` | `prd/vaults-collaboration.md` §History |

**Also settled while building it, and not numbered:** the history surface is a **right-hand drawer beside the editor**, mirroring `AgentPanel` — *not* a third `viewAtom` entry (history is *about* the open note, not a peer surface to notes/board) and *not* a modal, because **no modal/dialog/`role="dialog"` primitive exists in this codebase** and inventing one is a different slice; `window.confirm` (the `MembersSection` precedent) carries the restore confirmation, and it says what restore actually is — an edit every member sees, since `replaceAllText` goes through the live room, not a private undo. **No manual-snapshot button:** `snapshots.take` hardcodes `reason:'pre-agent-write'`, which is correct because its only caller *is* the agent path; four `SnapshotReason` values (`manual`, `pre-offline-merge`, `pre-reconcile`, `pre-git-ingest`) have no writer and are aspirational, not a contract — so `snapshotLabel` must not assume an unseen reason is impossible, and nobody should add a "save a version" button just because `manual` is sitting there (the PRD asks to browse and restore). **Restore refetches the list but not the doc** — the open editor updates itself over CRDT, while the list must refetch because the restore just minted the `pre-restore` row, and a timeline missing the undo of what you just did is missing it at the one moment it matters. **Left as an open question rather than fixed here:** `restore` writes the edit and takes its `pre-restore` snapshot **after**, with no transaction spanning both, so a throw between them leaves the edit committed with no undo — content is correct and the window is small, and widening a UI slice into transactional snapshot semantics is how it stops shipping.

**D55–D56 allocated 2026-07-16** for file-tree ops.

| Global | Decision | Cited in | Consolidated into |
|---|---|---|---|
| **D55** | **Delete surfaces backrefs *before*; tombstones *after* are deferred.** FR-12 wants both — a warning that N notes link here, and `[deleted note]` where those links now dangle. The first half is buildable now and is the half that **prevents** the loss: `notes.backrefs` was built, tested and **uncalled**, and one query turns "delete this?" into "1 note links here and will be left pointing at nothing: • projects/plan.md (2×)". The second half is not a small addition: **D27 says no cascade**, so a dangling ref survives by design — but the client rendering of that has **no host surface to render into**. Tombstones live in `related[]` (`shared/task-file.ts` — a ref whose doc is gone serializes as a raw id so the link survives a round-trip), and `related[]` is **invisible in the renderer**: `TaskDetail.tsx` has no relations row at all, and the strings `tombstone` / `[deleted note]` do not exist in the codebase. So "render tombstones" is really "build the relations surface", a separate known gap that must not be smuggled in behind a delete button. **Shipping the warning without the tombstone is not half a feature** — it is the preventive half, and strictly better than today's tree, which cannot delete at all and so warns about nothing. **The deferred half shipped the same day (D57):** the relations row exists now, so `[deleted note]` renders into it and FR-12 is whole. The deferral was correct and it was also short — it named the exact blocker, and naming it is what made it collectable. `namedBackrefs` joins `srcDocId` against `docsAtom` client-side (backrefs returns no path) and **never renders `undefined`** at someone about to make a delete decision — a link can come from a doc this vault's list cannot see. | `renderer/components/FileTree.tsx`, `renderer/state/vaults.ts` | `prd/notes-editor.md` FR-12 |
| **D56** | **Folders stay implicit, and the tree hides vestigial empty ones rather than deleting their rows.** You make a folder by naming a path (`ensureAncestorFolders`); there is **no `createFolder`/`deleteFolder` anywhere** and this slice adds none. **But shipping delete creates a problem that could not exist before:** deleting the last note in a folder leaves an orphan `folders` row, and `buildTree` renders it as an empty folder you cannot remove — and until now you could not delete a note from the tree at all, so nobody could make one. **Why not GC the row: `ON DELETE SET NULL` (D38) means deleting a folder row *unfiles every task pointing at it as its `area`*.** A folder with no docs can legitimately still be a board lane full of tasks — docs and tasks are different populations over the same folders — so a tidy-up in the notes tree would silently empty a lane on the board. **The fix is display and it is confined to the tree:** `buildTree` drops folder nodes with no descendant docs. Since nobody can deliberately create an empty folder, an empty folder is *always* vestigial, so hiding it loses nothing, and `foldersAtom` keeps feeding the board's lanes from the same untouched rows. Verified live: the emptied folder left the tree and the folder-row count was unchanged. | `renderer/lib/tree.ts` | `prd/notes-editor.md` FR-13 |

**Also settled while building it, and not numbered:** **`renameFolder` gained `renameNote`'s conflict check** — it had **none**, so renaming `a` onto an existing `b` silently *merged* them and a contained-doc collision surfaced only when the `docs` unique constraint fired **mid-loop**, leaving a half-moved folder. Unreachable until there was a button, then data loss; "wire what exists" does not mean wiring a known trap to a button. It rejects **before** moving anything, which is the only defence available: there is no transaction in `rename.ts` and there cannot be one, because `rewriteReferences` edits CRDT docs through the live relay and no database transaction rolls back a Yjs op. (The file header claimed "all SQL runs in one transaction" — never true of either function in it; corrected.) The guard checks the destination **folder row and every doc destination**, and mutation-testing is why: dropping the doc half failed nothing at first, because every path this codebase can reach has a folder row (all three `docs` writers call `ensureAncestorFolders`, and the daily archive moves through `renameNote`, which does too) — so the test now builds the degenerate state by hand and the check stands as the guard against a fourth writer that forgets. **Rename is also move** and there is **no drag-and-drop**: rename-to-path covers the semantics, and `BoardView`'s drag is a different shape (a drop target per cell, D42) that would not port. Affordances are **hover-revealed with an inline rename input**, because `onContextMenu` appears nowhere in the renderer and a menu primitive is net-new; `window.confirm` carries the delete, matching `MembersSection`. **`renameTarget`** keeps "can this row be renamed" out of the JSX — `buildTree` synthesizes prefix-only folders with `folderId: null` while `renameFolder` takes a folderId, so some rows are structurally un-renameable and must simply not offer it (do **not** paper over it by renaming the contained docs one by one — that is `renameFolder`'s job, and it does it with one link-rewrite pass and one up-front conflict check). Finally, **errors are surfaced**: the create form voided its promise, so a `CONFLICT` meant the row silently never appeared, and rename/delete both have reachable server errors.

**D57 allocated 2026-07-16** for the task detail's recurrence editor and relations row.

| Global | Decision | Cited in | Consolidated into |
|---|---|---|---|
| **D57** | **`related[]` gets a read-and-unlink row — not an editor — and it is where D27's tombstone finally renders.** `related[]` was writable from two sides (`@`-mention from a note, and the task file) and visible from none: `TaskDetail` had no relations row at all, so a link you made was a link you could not see. `resolveRelated` joins the stable ids against `docs`/`tasks` — the record never stores a path (**D27**), so this join is the only place a relation becomes readable. **No "add" control:** relations are authored from the *other* side, and a picker here would be a third author for the same edge — the one it duplicates being the one that already reads naturally ("`@`-mention the task in the note it is about"). **Unlink exists, and is not symmetry:** D27 means a delete does not cascade, so a dangling ref is permanent unless something removes it, and without an `×` the only way to clear one would be to hand-edit the task file. **The tombstone is a designed state, not an edge case** — `refToFile` deliberately round-trips the raw docId rather than dropping the ref, because dropping it would silently delete the link on the next inbound file write. A ref outliving its note is therefore *intended*, and the only honest rendering is to say `[deleted note]`. This closes the half of FR-12 that **D55** deferred hours earlier on exactly this blocker. `email`/`event` are representable but have no source to resolve against (phase 2) and are **not** marked missing: we cannot look, so calling them deleted would be a lie. | `renderer/state/tasks.ts`, `renderer/components/TaskDetail.tsx` | `prd/tasks.md`, `prd/notes-editor.md` FR-12 |

**Also settled while building it, and not numbered:** **recurrence gets a builder, while its sibling `reminder` keeps taking its grammar verbatim** — and the asymmetry is the model's, not a lapse. A reminder *is* a string (`1d`, `2026-07-20T09:00`) with a parser whose error message doubles as the format doc; a `Recurrence` is a **map** (`{frequency, interval, weekdays?, endDate?}`) with no text form anywhere in `shared`, so inventing a grammar here would mean inventing a parser too, plus a second way to say what the model already says structurally. **An empty weekday selection is stored as absent, never as `[]`** — `nextWeeklyWeekday` returns `null` on an empty set, so a literal empty list would silently stop the recurrence rather than mean "no weekday constraint". **A recurring task with no due date says so**, because `nextDue` has nothing to advance from and the rule would otherwise look set and simply never fire. Nothing client-side computes a next date: roll-forward is the server's single path (`tasks.complete` → `nextDueCatchup`), and this only states the rule.
