# Decision inbox

New load-bearing decisions land here first, as lightweight ADRs (context, decision, why, rejected alternatives). Once agreed with Nicolai, each entry is **consolidated natively into the owning living doc** — the relevant PRD, `architecture.md`, or `vision.md` — and purged from this file. The cycle then repeats.

The living docs are the truth; this file is only the staging area.

**D80 and D81 are agreed and not built** (2026-08-22); their rows are below and they are the only entries left in this inbox. **Next free is D96.** D91 through D95 were agreed and built on 2026-09-09 and never sat in this inbox; their rows are in the table. **D90 is skipped, not spent** — nothing in the repo ever claimed it, and the handoff that carried the editor work forward recorded it as taken, so it is cheaper to lose a number than to risk two decisions wearing one. D86 and D87 were built on 2026-08-23 and 2026-08-24 and are consolidated. **D88 and D89 never entered this inbox** — both were designed straight into `specs/` and agreed there, so their rows in the table record where the design lives rather than where prose was folded to. D79 was built and consolidated on 2026-08-21; D75, D76, D77 and D78 on 2026-08-20. Where each spent number's prose now lives is in the table below.

---

## D80 — an app's state is a file it owns, and sensitivity is a location rather than a cipher

**Context.** [`prd/vault-apps.md`](prd/vault-apps.md) §State has been open on purpose since D74: slice 1 shipped no state API at all, and the PRD refused to guess because "every framing that picks one for all apps is wrong for half the examples in this document". Real apps have now named the two shapes. A retro board and a poll are **shared by nature** — the state is the point of the app. A CSV explorer's filters, and a personal-finance app holding months of real transactions, are **nobody else's business**, and the second one is the case that settles the design: it is not merely private, it is data a user would be alarmed to find in a repo.

**Decision.** State is **per app and declared in `app.yaml`**, which is already the registration marker and already the place a manifest carries what Holi cannot derive. Two kinds:

- **`state: local`** — a real **SQLite** database (`node:sqlite`, already in the tree for the Google cache), under `userData`, with its **path and a size cap declared in the manifest**. It never enters the repo. This is the default and the one a finance app uses.
- **`state: shared`** — a **text** document (JSON/NDJSON) in the app's own folder, committed like any vault content.

**Why sensitivity is a location, not encryption.** Encrypting shared state was considered and rejected: with no server there is no key distribution, so a shared vault's key would be a password in a file that every member already has — which protects against nobody who matters. Ciphertext also inherits every problem the binary database had: git cannot merge it and a human cannot read it in a diff. **The vault already has the right mechanism**, and it is D65's: `.local.` means machine-local, and it is what keeps `USER.local.md` and every token out of git today. Sensitive state is *local* state; that is the whole answer, and it needs no new machinery.

**Why shared state is text and not SQLite.** A committed `.db` is a binary that changes wholesale on every write. Autosave commits every three seconds ([`prd/vaults-sync.md`](prd/vaults-sync.md) FR-4), so an app writing state would produce a stream of multi-kilobyte blobs, and two people editing at once is a conflict **git cannot 3-way merge** — one person's afternoon gets chosen wholesale, silently. Text merges, diffs, and fails visibly.

**Rejected.** *SQLite for both with the `.db` gitignored* — one API and one engine, but `state: shared` would then be a promise nothing keeps, and the retro board still could not be built. *SQLite for both, committed, size-capped* — simplest to build and exactly the shape first proposed; the commit stream and the unmergeable conflict are what rule it out. *Local only* — no conflicts and no sync design, but it leaves the two apps this feature exists for still unbuildable.

**Still open:** whether `state: shared` needs a write-through API shaped like a document or like a log, which is the difference between last-writer-wins and a merge that keeps both. That is a question for the first shared app.

---

## D81 — a slash command is a small program in the apps sandbox, not a shell

**Context.** [`prd/notes-editor.md`](prd/notes-editor.md) FR-9 has slash commands as a fixed pair — `/todo` and a table insert — "extensible via the provider registry", which is a seam nobody outside the app can reach. Nicolai: *"a user can literally assign virtually any command to a slash command… think of them as small programs."* The named uses are text transforms (insert a date, a calculator, expand a template), **vault actions** (create a task from the selection, link a note), **network** (fetch a ticket title), **asking the vault agent** for a paragraph in place, **starting a mail reply**, and **embedding an app inline**. Shell access was considered and is deliberately not in that list.

**Decision.** A command is a **small JS module the vault commits**, run in the **same isolated origin vault apps use** (D74: a per-app `holi-app://` origin, `allow-scripts` never with `allow-same-origin`), handed the buffer and the selection and returning text or an action. It is a real program — loops, dates, arithmetic, `fetch` — with **no filesystem, no shell and no node**. The bridge it gets is the apps bridge plus what an editor command needs: the selection, the vault actions apps already have, an **agent query**, and a **compose** call for the mail case.

**Why the sandbox rather than real scripts.** A vault is shared, and a committed command is code that arrives on your laptop when you pull. Running it for real is precisely the escalation **D76** refused for hooks — the reason `core.hooksPath` is not used and the hook is written to `.git/hooks/` instead, because "a synced `.holi/git-hooks/` lets a teammate push a hook onto your laptop". The sandbox keeps the power that was actually asked for (every named use case fits inside it) and drops the one that cannot be accepted from someone else.

**The network is in, and its cost is stated rather than hidden.** A command that reads your buffer and posts it somewhere is the shape to guard against, and it is the same exfiltration cost D74 accepted for apps, for the same reason: refusing the network makes half the useful commands impossible, and a vault's content is already readable by anyone who can read the vault.

**Rejected.** *Real programs, machine-local only* (`.holi/commands.local/`) — full power and safe because nothing another person wrote can arrive there, but then a command is not vault content: the agent cannot write one for the team and it does not travel with the vault, which is most of the point. *Real programs, committed, enabled per vault like hooks* — the most powerful and the closest reading of "literally any command", and it puts executable code one commit away from running on a teammate's machine. *Reusing `.claude/commands/`* — already in every vault and already known to the agent, but those are prompts for an agent, not editor insertions, and a single list mixing the two would mean two different things by one gesture.

**Still open:** whether an inline agent query is a command's own call or the drawer's seeded prompt, and whether an embedded app is a widget in the note or a link that opens a tab. The second one touches [`prd/vault-apps.md`](prd/vault-apps.md) §Note-embedded widgets, which is currently deferred.

---

## Number allocation — **next free is D96**

Living docs carry decisions as **prose, never as numbers**. D-numbers exist for two purposes only: **code comments** and **git history**. So this ledger is the one place that records which numbers are spent. Check it before allocating.

**Spent and purged from this file, with where each one's prose now lives** — a number vanishing from the inbox means it was consolidated, not withdrawn:

| | Consolidated into |
|---|---|
| D89 — the vault remembers in a directory, not in one file | Design of record: [`specs/2026-09-05-vault-memory-design.md`](specs/2026-09-05-vault-memory-design.md). Plan: [`plans/2026-09-05-vault-memory.md`](plans/2026-09-05-vault-memory.md). Issue: [`syv-ai/holi#3`](https://github.com/syv-ai/holi/issues/3), whose comment records what was adopted and what was not. **Agreed 2026-09-05, NOT built.** Never sat in this inbox: it was designed straight into a spec against #3's evaluation of understory. Settles that `MEMORY.md`'s one file with a character budget and a request becomes a **typed `memory/` directory at the vault ROOT** (content, not plumbing, and where `MEMORY.md` already sits), whose `index.md` a fourth pre-commit transform regenerates and which a `SessionStart` hook summarises so a session starts knowing what it knows. Personal memory is `memory/x.local.md` and needs no new machinery: D65's marker plus the seeded `*.local.*` ignore already do it, and the committed index lists tracked memories only so a personal one appears in no committed file. **Reading OKF v0.2 dissolved the sharpest question in #3**: `type` is the only required key, broken links are explicitly legal, and everything else is soft guidance, so nothing here could earn a veto and FR-9 stands untouched — the indexer **maintains** (missing `title` → H1 → filename, missing `description` → first sentence, missing `type` → `note`) and never enforces. **The shape, never the label**: OKF links are markdown and Holi's are wiki-links, so a wiki-linked directory is OKF-shaped and not conformant, and claiming otherwise would be a claim we fail; the alternative would put a second link grammar inside `memory/` that `relink`, backlinks and the editor's chips do not understand. **No `log.md`** — git already is one, and a newest-first file conflicts on its first lines every time two members write memory the same afternoon, in a generated file no human wrote. **No `index.local.md`** — a gitignored file never stages, so the commit-boundary transform could not maintain it, and its only reader can read the sources. **Item 13 closes on one settings key**: Claude Code's `autoMemoryEnabled: false` in the seeded `.claude/settings.json`, merged into existing vaults the way `disableClaudeAiConnectors` is, in place of a second tree source and a read/write path outside path-safety containment. `autoMemoryDirectory` would point that memory at the vault instead and is refused twice: Claude Code ignores it in checked-in project settings by its own rule, and its `[[slug]]` links address memories by name where Holi's address vault paths. **Verification owed** before the key ships: that `autoMemoryEnabled` is honoured from projectSettings, read out of the 2.1.261 binary rather than a document. Scope is #3's items 1 to 3; lint-as-proposals and the vault-wide graph view stay open |
| D88 — an agent turn is a commit range | Design of record: [`specs/2026-09-02-agent-turn-review-design.md`](specs/2026-09-02-agent-turn-review-design.md). Plan: [`plans/2026-09-02-agent-turn-review.md`](plans/2026-09-02-agent-turn-review.md). Issues: [`#4`](https://github.com/syv-ai/holi/issues/4), [`#7`](https://github.com/syv-ai/holi/issues/7), [`#8`](https://github.com/syv-ai/holi/issues/8). **Agreed 2026-09-02. #4 BUILT 2026-09-09**; #7 and #8 are the panel's later refinements and remain open. Also never sat in this inbox. Settles that the agent's writes get a **review after the turn** rather than a gate before it — [`prd/vaults-sync.md`](prd/vaults-sync.md) §Non-goals rules out an outbound gate and Claude Code's own permission prompts already ask — and that the unit is a **commit range**: `base` at turn start, the settle commit's sha at turn end, the changed files from `git diff base..end`. A range rather than a working-tree diff because a working-tree diff keeps growing and would attribute a day of your own writing to the agent; a range rather than a tool-level record because git catches the files the agent changed through `Bash` (a `sed`, an `mv`, a script) that a `Write\|Edit\|MultiEdit` matcher never sees. The bracket it rides on already exists: the seeded `UserPromptSubmit` and `Stop` hooks that suspend the sync loop for the turn. A revert is a **new commit**, never a history rewrite, matching `history.restore` | **What the build added.** (a) `commitNow` is called AFTER the vault resumes, not before: `commitAll` refuses to run while the vault reads as paused, so committing first would record nothing and strand the vault paused — the failure the safety cap exists to prevent. (b) The recording is a **passenger** on the turn bracket: fire-and-forget with every rejection swallowed, because the function it lives in answers a hook request that must return an empty body immediately, and a failed record must not disturb the sync resume it shares a body with. (c) An **empty file list is not "nothing changed"** — `turn-log` drops a record whose base equals its end, so an empty list against a real turn means the commits it names are unreachable (a reset, a re-clone), and the panel says that rather than showing a turn that appears to have done nothing. (d) The plan wired the diff's `onResolve` straight to the revert mutation; `DiffView` reports every document change and an editable merge view includes typing, so that is **a commit per keystroke**. The resolved text is held and written by a control at the foot instead, which is History's own shape and gives a whole file's accepts and rejects one commit. (e) The footer chip watches `working` go **true → false** rather than polling: that edge IS the end of a turn, and reloading while it is still true would read the previous turn, because this turn's record is written in the same handler |
| D91 — live preview reveals the ELEMENT, not the line | [`prd/notes-editor.md`](prd/notes-editor.md) §No animation layer, the live-preview bullet in §What this is, and FR-2's reveal rule. Agreed and built 2026-09-09, so it never sat in this inbox. **Narrows D22**, whose row is long gone with D1–D59 but whose rule was still the one in the code: the LINE the primary selection touched rendered raw. On a real line that meant putting the caret next to a word to fix a typo and watching every chip, image and pair of `**` on that line fall back to source at once. Settles that the unit is the **element**, that touching an **edge counts** (a caret resting at either end keeps it raw, so the `**` you have just closed do not vanish under your fingers and shift the line four characters left), and that when elements nest only the **innermost** one opens — with the caret on the link in `**bold with [[a link]] inside**` you are not on the bold, you are on the thing inside it. `revealedSpans` in `editor/livePreview.ts` is the whole rule, pure and on numbers. **Two exceptions keep a line's worth of scope, and both are the same exception**: a heading's `#` and a list item's marker belong to a LINE rather than to a word, so their reveal span is the line — scoping them to the two columns they occupy would mean the caret had to land on the marker before you could edit the heading. **What the build found, in the running app**: the markdown parser reads the inner `[notes/plan.md]` of a `[[wiki link]]` as a shortcut `Link`, and under the innermost rule that phantom shadowed the link it sat in, so a chip would never open again. The wiki grammar owns its range and the tree's reading inside it is discarded — the same rule the frontmatter region already had. **The cost is stated rather than hidden**: FR-3b gets louder, because one word on a line can now grow two `**` while the rest of the line stays put and shifts. That is inherent to the finer unit, not a defect of it |
| D95 — settings are a tab, and the ritual asks a subset of them | [`prd/vaults-sync.md`](prd/vaults-sync.md) §Settings are a tab. Agreed and built 2026-09-09 for [`#16`](https://github.com/syv-ai/holi/issues/16); never sat in this inbox. Settles that **no setting being editable from the running app** ends: the onboarding ritual was the only writer and it is a one-shot flow you cannot get back to, so changing a hook or the editor font meant hand-editing JSON. **A tab, not a modal**, because a modal blocks the window exactly while you want to compare a setting against the vault it applies to; being a `SingletonTab` also makes it splittable, closable and one-per-window for free. **One list, three readers**: the pane renders `VAULT_SETTING_DESCRIPTORS` and writes through `settings.write`, which is the ritual's only writer, so the two cannot drift into writing different shapes. The new `askedAtBirth` flag splits "every setting" from "every question" — the ritual and the seed take `RITUAL_SETTING_DESCRIPTORS`, because the ritual is four acts long and every row is one more thing between somebody and their first note. `editorFont` forced it: D87 deliberately gave it no ritual row, and it is obviously a row in a settings pane. **The reload question is answered rather than dodged.** The cache's own docstring said re-reading settings mid-session would mean answering "what happens to the tab you are looking at"; the answer is that most of them already apply live — `hooks` and `maxCommittedFileBytes` are read by main on every commit, `colorScheme` re-applies through `useVaultTheme`, `editorFont` is a CSS custom property — so a write forces the cache and the app follows. `landing` is the one exception and its row says so, because it describes what happens when a vault OPENS and this one already did. **Each row names its layer** (`vault` / `this machine`), or a pane could silently commit a machine-local preference and defeat the layer it was writing to. The resolver's `warnings[]` are surfaced beside the setting they name, which nothing displayed before. **The gear now means one thing**: it opens this tab, and the read-only identity/members/visibility panel it used to open — which is not preferences — is reachable from inside it, through `vaultPanelOpenAtom` rather than `Shell`'s local state. **Deliberately NOT done, each for a reason rather than for time**: `maxCommittedFileBytes` gets no control, because D85's argument against freezing a number into every vault was about the seed and has not been re-argued for an explicit choice in a pane; theme tokens get no controls, and the pane links the file; and the other `.holi/*.json` files stay where they are, because `vault.json`, `icons.json`, `seed-state.local.json` and `context.local.json` are state or data that happen to be JSON, and moving config files in a committed vault breaks every vault that exists in order to buy tidiness |
| D94 — a note gets its frontmatter however it arrived, and the bar is a fact about being markdown | [`prd/vaults-sync.md`](prd/vaults-sync.md) FR-9 and [`prd/notes-editor.md`](prd/notes-editor.md) §Frontmatter reveal control. Agreed and built 2026-09-09 for [`#17`](https://github.com/syv-ai/holi/issues/17); never sat in this inbox. **Two symptoms, one mechanism.** Only `createNoteAtom` scaffolded, so every other way a `.md` lands in a vault — an agent's `Write`, a drag-and-drop import, a file made in another editor — produced a note with no `created` and no `tags`; and because "N chars · Last updated …" is *built only to label a collapsed frontmatter block*, such a note also had no bar at all. **The bar half** decouples the two: with no frontmatter the widget is inserted above the first line rather than replacing a region (`side: -1`, so a caret at 0 lands in the body), carries no chevron, and offers no way to add a block, since the transform does that on the next commit and two ways to write the same four lines is one too many. It needed no new logic — the count already came from `doc.slice(bodyStart(doc))` and `bodyStart` is 0 with no block — and it reaches markdown only by construction, because a non-markdown text file opens in the plain stack. **The transform half** is a FOURTH transform rather than a fourth rule inside `normalize-md`, whose stated contract is "changes the author would not have noticed being made": writing four lines at the top of a note is the one visible change in the pipeline, and it argues for itself separately. It runs on the **added set only**, which settles the question the issue flagged as blocking — a file arriving on `git pull` is committed by the pull and never appears in a staged set, and one that has been here a year is `modified`, so "rewriting somebody else's note" cannot arise; what is left is exactly the issue's own list, all local creations. That also makes `created:` honest for free: an added file has no first commit yet, so today IS its creation date, and the issue's "derive it from the first commit" requirement dissolves rather than being skipped. **Markdown is not the same as a note.** `wantsScaffold` excludes the agent surface (`CLAUDE.md`, `AGENTS.md`, `MEMORY.md`, `USER.local.md`, `.claude/`), because those are read verbatim as instructions and a `created:` block there is prompt text — the argument D82 made when it refused frontmatter for a note's icon; task files, because `serializeTaskFile` owns their frontmatter and `normalize-md` already canonicalises it; and anything hidden. A daily note needs no rule: it is born with a block, so the scaffold is a no-op. **`decideReload` is deliberately NOT taught to recognise this write**, unlike `normalize-md`'s tidy. A transform rewrites a file after the save that triggered it, and this one lands on line 1 — but a file in the ADDED set is one whose very first commit this is: the agent and the import are not writing into something the user has open, and a note made with the `+` arrives already scaffolded and passes through as a no-op. The clean-buffer path reloads it, `merge3` handles a prepend against a dirty buffer, and what remains is someone creating a `.md` in another editor and typing on its first line before the first autosave commit. Stated rather than defended against. **One union, not two**: `TransformName` was written out in `hooks/runner.ts` as well as in `@holi/shared`, and adding a fourth transform to only one of them is how that was found; main re-exports it now |
| D93 — a table cell is reached by a highlight style, not by decorations | [`prd/notes-editor.md`](prd/notes-editor.md) FR-10, and [`not-built.md`](not-built.md) §Notes & editor for what is still missing. Agreed and built 2026-09-09 for [`#11`](https://github.com/syv-ai/holi/issues/11); never sat in this inbox. **The issue's premise was half right.** It said to pass the inline stack into the cell editors through the widget's own `extensions`, which is correct and is done — a cell you have clicked into is a real nested `EditorView` and now gets `livePreview` plus the three facets it reads, so editing a cell behaves like editing anywhere else, and moving `markdownTables()` inside `baseEditorExtensions` is what makes those facets reachable. What the issue did not know is that **an unfocused cell is not an editor at all**: `codemirror-markdown-tables` renders it as a `contenteditable` div whose spans it classes from `highlightingFor(rootState, tags)`. Since every cell in a table is unfocused except at most one, fixing only the editors would have left the bug exactly as reported. So the second half of the fix is a **HighlightStyle**, which is the only thing that reaches that path. Settles that markdown's prose tags are classed for cells: `strong`, `emphasis` and `strikethrough` borrow live preview's own classes, since they land on precisely the text it marks; `monospace` and `link` get classes of their own styled ONLY under `.tbl-cell-view`, because `monospace` is also `CodeText` (borrowing `.cm-inline-code` would put an inline-code background on every line of every fenced block, which is how the fence test caught it) and `link` covers a link's brackets, which the body draws as punctuation. **A cell can be styled but not concealed**, because concealing is a decoration — what it does instead is exploit inheritance: a mark takes the class of the node it sits in, so a `**` carries `cm-strong` while a link's `[` carries `cm-cell-link`, and `.cm-md-mark:not(.cm-cell-link)` is the whole rule. **A link keeps its brackets deliberately**: markdown parses the inner pair of `[[…]]` as a shortcut link and the outer pair is plain text, so hiding one pair of two turns a wiki-link into something that reads as broken. **`inlineOnlyFacet` is mostly belt and braces** — the package already removes `ATXHeading`, `Blockquote`, `BulletList`, `FencedCode`, `HorizontalRule`, `IndentedCode`, `OrderedList` and `SetextHeading` from a cell's grammar, so what the facet genuinely covers is the alphabetic-list scan (a regex over lines, not a parser rule, and it would indent a cell reading `a. thing`) and the image widget (inline to markdown, a picture to us). It gates D91's span pass too, or a heading the walk is ignoring would still shadow the element the caret is on. **Checked in the running app** on a detached editor built from the dev server's own modules, so nothing was written to a vault: the marks hide, and a click into a cell still lands on the source offset it looks like it should — the package maps a click through its own model rather than through rendered widths, so hidden characters do not shift it |
| D92 — a heading's `#` slides | Same section. Agreed and built the same day, alongside D91, and the **one carve-out in FR-2's "no animation layer"**, which had until now refused every animation on the grounds that animating CodeMirror decorations pegs its measure loop. This is affordable because it is not a decoration animation: the mark is a `Decoration.mark` that **never changes**, so CodeMirror keeps the same DOM element across a selection move and only CSS runs. A `Decoration.replace` — what concealed it before — takes the text out of the DOM, so there was nothing left to transition and the swap could only blink. The open/closed state rides on the LINE's `cm-heading-raw` class for the same reason: flipping a class on the mark itself would rebuild it. **No measurement**, which is what killed the last attempt at a marker width in this editor (`cd4cf31`): `width: 0` ↔ `width: auto` under `interpolate-size: allow-keywords` makes the browser measure one to six `#` in the vault's own face, a width `ch` cannot express because `ch` is the width of a zero. Duration is `--duration-base`/`--ease-settle`, the app's existing motion vocabulary, rather than a number of its own. **Found by him within minutes of it landing**: a closed h2 stood three lines tall. `.cm-line` is `white-space: pre-wrap`, the mark inherited it, and at `width: 0` the `## ` wrapped one character per line inside its own clipped box — `overflow: hidden` clips sideways and keeps the height that wrapping produced. `white-space: pre` on the mark is the fix, and it is load-bearing rather than tidy. **Reported by him minutes later, twice.** First: every heading in a file animated its marks shut as the file opened, because CodeMirror creates the mark span and settles its style in two steps, so the element's first resolved width is `auto` and the rule closing it reads as a transitionable change — `@starting-style` does not help, since this is not an insertion. The transition is therefore declared only under a `.cm-heading-sliding` class the plugin adds on a **selection change** and drops shortly after, which `ViewPlugin.update` running before `docView.update` makes possible; a mark rendered by a scroll or a file open finds transitions off. Second, and the deeper half: the caret stayed where the text had been and only snapped across when he typed. `drawSelection` reads coordinates once per update, and `view.requestMeasure()` cannot help because a `layer` recomputes only when its OWN measure request is queued — on a transaction or a doc-view update, and there is no API to poke one. A transaction per frame would redraw it and would also rebuild every decoration in the viewport fifteen times for one caret move, which is the cost FR-2 exists to refuse, so `editor/heading-slide.ts` does the cheap half instead: one `coordsAtPos` and one `left` per frame, only while a `transitionrun` on a heading mark is outstanding, capped so a lost `transitionend` costs idle frames rather than a loop. Measured in the running app against CodeMirror's own recomputation: **the same pixel** |
| D60 — the vault is a GitHub repo; there is no server | `vision.md`, `architecture.md`, `glossary.md` and every PRD (2026-07-21). Held open afterwards only to track code that had not caught up; the last of it, the reconcile → agent-drawer handoff, was built on 2026-07-27 and is described in [`prd/agent.md`](prd/agent.md) §The agent as merge resolver. D60 is spent in full |
| D61 | spent amending D60 pt 2 (auto-push) — see D60 |
| D62 — text-first by authorship; binaries are assets | `vision.md` §Key design choices, [`prd/notes-editor.md`](prd/notes-editor.md) §Images and other binaries, [`prd/pdf-export.md`](prd/pdf-export.md), and [`not-built.md`](not-built.md) (the surviving blob-storage question, and the import-conversion stub D62 superseded — that stub is deleted, and its reasoning is in this row plus git history) |
| D63 — a task lane move is a note-style rename | [`prd/tasks.md`](prd/tasks.md) §Board UX |
| D64 — a vault's theme is a whitelisted token map | [`architecture.md`](architecture.md) §9, §10; [`specs/2026-08-02-per-vault-theming-design.md`](specs/2026-08-02-per-vault-theming-design.md) is the design-of-record. **Amended 2026-08-20, three times, each widening what a token reaches rather than what a theme may do.** (a) **`brand` joined the whitelist**: `--primary` is the brand as a *fill*, paired with `--primary-foreground` on top of it, and on a dark-first theme a fill dark enough to carry near-white text is far too dark to BE text — sky-700 on neutral-950 measures **3.38:1**, under AA. `--brand` is the same brand as *text* (**9.09:1**); all 15 `text-primary` sites moved onto it, it is in `APP_BASE_TOKENS` so vault apps get it, and the authoring skill teaches the split. A vault recolouring the brand should set both — setting only `primary` recolours the fills and leaves the text behind. (b) **`scrollbar-thumb(-hover)` now reaches every scroll container**, not the three that wore a `.holi-scroll` class; the paint rule is global, so the token finally means what this whitelist always said it meant. `color-scheme` is stamped per theme alongside it, and is deliberately NOT a vault knob: it follows `data-theme`, and it is what stops everything Holi does not paint itself from defaulting to light. (c) **`divider` split the chrome hairline off `border`**: one token was drawing both the edge of an *object* (card, chip, popover), which has to be seen, and the seam between two pieces of *chrome* (pane splits, the sidebar's edge, panel-header rules, the footer), which only has to be noticed — at the object weight a window of panes read as a grid of boxes. 18 sites moved, four of which had already hand-rolled it as `border-border/50`. Its default is derived (`color-mix(in srgb, var(--border) 55%, var(--background))`), so a vault recolouring `border`/`background` gets a matching divider for free; setting it outright takes a flat colour, as `color-mix` is not a value the validator accepts. No amendment adds an expressive power — the vocabulary still contains nothing that can move, size or re-space anything, which is the whole of D64's guarantee |
| D65 — local-ness is legible from the name | [`architecture.md`](architecture.md) §3 + config layering, [`glossary.md`](glossary.md), [`prd/agent.md`](prd/agent.md), [`prd/auth-identity.md`](prd/auth-identity.md) |
| D66 — document-templates lives under a name that says what it is for | [`prd/agent.md`](prd/agent.md) |
| D67 — one main-held Google connector; the agent reaches it via a CLI, not MCP | [`prd/google-mail-calendar.md`](prd/google-mail-calendar.md) §The engine, §Calendar, §How a message is rendered, §Rejected; [`prd/agent.md`](prd/agent.md) §Tool surface; [`prd/auth-identity.md`](prd/auth-identity.md); [`prd/tasks.md`](prd/tasks.md) |
| D68 — mail is read-write within a bounded set | [`prd/google-mail-calendar.md`](prd/google-mail-calendar.md) §Goals — as built, §The engine; [`prd/auth-identity.md`](prd/auth-identity.md) (the scope-widening trap) |
| D69 — a cached list is defined by every filter that narrows it | [`prd/google-mail-calendar.md`](prd/google-mail-calendar.md) §The engine, §How a message is rendered |
| D70 — the agent may do anything the user can undo | [`prd/agent.md`](prd/agent.md) §Tool surface, §Permissions; [`prd/google-mail-calendar.md`](prd/google-mail-calendar.md) |
| D71 — the mail composer: markdown is the source | [`prd/google-mail-calendar.md`](prd/google-mail-calendar.md) §Composing |
| D72 — a vault agent inherits the vault, not the machine | [`prd/agent.md`](prd/agent.md) §Config layering, §Runtime, §Auth, §Permissions |
| D73 — a mail thread and a calendar event are joined by the invite's UID | [`prd/google-mail-calendar.md`](prd/google-mail-calendar.md) §Goals — as built, §How a message is rendered |
| D74 — a vault app is a web app the user wrote, bounded by its own origin and barred from the agent surface | [`prd/vault-apps.md`](prd/vault-apps.md) §Trust & isolation, §Anatomy, §Runtime & surfaces, §The `holi.*` bridge, §State deferred, §Slice 1; [`glossary.md`](glossary.md) §Agent surface. Settles the isolation model (a per-app `holi-app://` origin, `allow-scripts` never with `allow-same-origin`), the reach (all vault content except `AGENTS.md`/`CLAUDE.md`/`MEMORY.md`/`USER.local.md`/`.claude/`, read *and* write, because a writable send-gate hook is an app escalating to the assistant), network allowed with its exfiltration cost stated, bridge + theme injected on serve, the tab union's singleton-by-subtraction bug, no manifest in slice 1, and personal apps split by location rather than by the `.local.` marker. **Deliberately does not decide where app state lives** — that is per-app rather than per-platform, so slice 1 ships no state API and the question waits for real apps |
| D75 — Holi owns its own documentation; a managed file it wrote is refreshed, not frozen | [`prd/agent.md`](prd/agent.md) §Permissions (the managed-file bullets, beside D70's "a migration that never happens"). Settles the managed/once split of `SEED_FILES`, the sha256 in a machine-local `.holi/seed-state.local.json`, that an absent record means never overwrite, that a byte-identical file is adopted, and that `--force` reaches managed files only. Its `holi seed refresh` surface is in [`prd/vault-apps.md`](prd/vault-apps.md) §Slice 2 |
| D76 — vault hooks are a managed capability: Holi ships the code, the vault enables it | [`prd/vaults-sync.md`](prd/vaults-sync.md) §Committing FR-9 (the commit boundary, the never-block rule, the settings-are-data rule); [`prd/agent.md`](prd/agent.md) §Permissions (the agent-facing half). Settles the three transforms (`relink`, `archive-done` off by default, `normalize-md`), that a transform logs and lets the commit through while the large-file guard keeps its veto as an integrity check, the per-session circuit breaker, and the machine-local run log. **Amended in build**: the hook is written to `.git/hooks/` rather than `.holi/git-hooks/`, and `core.hooksPath` is not used at all — pointing it at the tracked tree would have silently disabled the large-file guard, and would have been the very escalation D76 was written to prevent, since a synced `.holi/git-hooks/` lets a teammate push a hook onto your laptop. `.git/hooks/` is machine-local by construction, so parts 1–2 (including the `isAgentSurfacePath` entry) are moot rather than merely unbuilt. **Not built**: the push to a live agent (part 4) — no such seam exists — and the status-bar floor. See [`not-built.md`](not-built.md) |
| D77 — a pane is where a document lives, and a document lives in exactly one of them | [`prd/notes-editor.md`](prd/notes-editor.md) §Split panes, §Tabs — preview vs pinned (the one-buffer rule), §The strip clips; [`prd/vault-apps.md`](prd/vault-apps.md) §Runtime & surfaces (Open in a New Pane). Settles the split that `panes[] → tabs[]` was shaped for on the first commit: a strip per pane, focus that follows the pointer AND the keyboard, an emptied split pane that removes itself while the last pane never does, and a pane-group layout that is deliberately not persisted. **The load-bearing half is the rule it strengthens**: one buffer per file was per-pane while there was one pane, and a second pane made it a hole — two `EditorPane`s over one path are two `base`s racing two autosave debounces, each reading the other's write as an external change. So the lookup spans every pane, every opener routes through it, and **⌘\ therefore cannot duplicate the active tab** the way VS Code and Obsidian do; it opens an empty pane and `openInNewPane` is the gesture that fills one. **Not decided**: whether a split survives a restart — tabs are not persisted either, and the answer belongs with that one |

| D78 — a tab is dragged, and a drag may only promise what it can do | [`prd/notes-editor.md`](prd/notes-editor.md) §Moving a tab (new), §Tabs — preview vs pinned (the singletons, and the one-buffer rule's move-vs-copy half). Design-of-record: [`specs/2026-08-20-movable-tabs-design.md`](specs/2026-08-20-movable-tabs-design.md); checked by hand in [`verification/2026-08-20-movable-tabs.md`](verification/2026-08-20-movable-tabs.md). Settles reorder-within-a-strip, cross-pane move, and edge-drop-to-split; that the payload is the tab's **identity** rather than its location, so `findTab` makes one function serve a reorder and a move; that a drag **pins** a preview tab (without it the next single-click destroys the tab just positioned); that a **reorder rearranges without navigating** while a cross-pane move focuses what it landed — a distinction the first draft of the spec missed, and the tests found; that the singletons **open** leftmost rather than living there; and that a clipped drop position is reachable because a drag substitutes its own index into `tab-window.ts`'s existing "this index must stay visible" rule. **The load-bearing half is what a drag refuses to offer**: a pane never highlights a drop that would be a no-op, so the pane a drag came from drops its middle, and a sole tab's three surrounding edges all go inert — **three**, because an edge shared by two panes is one gap, which is why `dropZones` asks the moves themselves (they return the workspace by reference when they would change nothing) instead of restating their rules and drifting from them. What it *does* offer is drawn **before** it is aimed at: both landing strips appear on `dragstart`, dim, because a target that materialises once you reach it teaches nobody the gesture. Rejected: a custom pointer-event drag (three times the code, and it fights `onPointerDownCapture`) and a jotai drag-state atom (two sources of truth for one drag, stale on a missed `dragend`). **Not built**: dragging a tab to a new window, between vaults, or any vertical split — the pane model has one axis |

| D79 — a task's dates are stamps you pick, not a grammar you type | [`prd/tasks.md`](prd/tasks.md) §Recurrence & reminders (the reminder-is-a-moment and `due`-may-name-an-hour bullets), §Board UX (`overdue`'s two rules), §The task file, §Edge cases (the minute-valued `nowAtom`). Design of record: [`specs/2026-08-21-task-datetime-design.md`](specs/2026-08-21-task-datetime-design.md); checked by hand in [`verification/2026-08-21-task-datetime.md`](verification/2026-08-21-task-datetime.md). Settles that `due` and `reminder` are one type — a local stamp, `YYYY-MM-DD` or `YYYY-MM-DDTHH:MM`, the time optional on both and its absence meaningful; that **a reminder is an absolute moment** and the relative grammar (`1d`, `2w`) becomes presets in a picker that resolve on choosing, so the file says when the notification happens rather than how to compute it; that `overdue` honours the time in **two rules**, a timeless due being late only once the day has passed; that recurrence carries the hour across a roll-forward while `until` stays a date, being a boundary on a rule rather than an appointment; and that one `DateTimePicker` composite serves all three fields over a hand-rolled month grid (`packages/shared/src/calendar.ts`) — no date library enters the repo. **No migration was needed**: nothing in the wild held a relative value, and a reminder that is not a stamp is **inert, never an error**, because `PATCH_READERS` is shared between write and read and validating on write would break the whole task on read. Rejected: a structured map like `recurrence` (nested YAML for the agent to write, and a migration), two fields (`reminder` + `reminder-time`, which only mean anything together), a day-granular `overdue` (which would make the hour on `due` decorative), and `react-day-picker` |

| D86 — the vault agent runs in its own silo | Design: [`specs/2026-08-23-per-vault-agent-silos-design.md`](specs/2026-08-23-per-vault-agent-silos-design.md). **Agreed and built 2026-08-23.** **Overturns the 2026-08-14 isolation spec §4**, which chose ONE shared `userData/agent-config/` for every vault. That spec's reasoning was right and its conclusion has gone stale: it showed that per-vault *history* comes free (Claude Code keys transcripts, prompt history and project config by working directory, all verified again on 2026-08-23), and concluded that per-vault directories bought nothing for the price of a `/login` each. It did not consider **capability**. `plugins/` is 444 files of marketplaces and installed plugins, keyed by nothing, so a plugin installed while working in one vault is available to the agent in every vault. Settles that each vault gets `userData/agent-config/<vault-slug>/`, resolved **per spawn** rather than per app launch, since the active vault changes while the app runs. The `/login` per vault is accepted and is deliberately **lazy**: the panel asks the first time the agent is opened in that vault, not during onboarding. An agentic onboarding was considered and does not work at this seam, because an agent needs a config dir, a login and a cwd before it can ask anything, so it moves the login earlier rather than removing it. **§6 becomes part of this**: it was never built (no `oauthAccount` check exists in main), so today an unauthenticated agent just prints `Not logged in` and the user is left to infer `/login`. Survivable once per install, not once per vault. Also fixes what surfaced it: Claude Code's theme lives in that directory's `settings.json` as `"auto"`, meaning *detect the terminal background*, which inside Holi's PTY has nothing to read, so the agent stayed dark while D85 took the app light. Holi writes `dark`/`light` from `activeModeAtom` instead, per vault, on every spawn | **What the build settled that the design left open**, four things: (a) the slug is `<sanitized remote>-<8 hex of sha1(remote)>` rather than the bare `projects/` convention the design suggested, because that convention is **not injective** — `syv/better-holi` and `syv-better/holi` both sanitize to `syv-better-holi`, and two vaults quietly sharing one config directory is precisely the leak D86 exists to close, so eight characters of legibility buy a collision that cannot happen rather than one that is merely unlikely. (b) The `/login` instruction lives in the **scrollback**, printed by main at spawn, not in the panel header — and that is D72's own argument rather than a reversal of it: `/login` fires no spawn, no turn and no exit, so a header badge goes stale the moment it matters, while a line printed at spawn is a log entry that stays true about that spawn. (c) The migration is a **whole-directory rename** into the `lastOpenedAt` head of the registry, staged through `agent-config.migrating` because a directory cannot be renamed into itself, guarded against clobbering an existing slot **before** the rename (checking afterwards finds nothing, because the rename carried the slot along), and resumable — an interrupted run leaves the staging directory, which the next launch finishes. (d) The theme's known limit is said **in the renderer**, by comparing the mode now against the mode at spawn, rather than by widening `AGENT_CONFIG_FILES` to fingerprint `.holi/settings.local.json`: that set excludes `*.local.*` on the stated grounds that it never syncs and so no collaborator's pull can change it, and widening it to catch a local theme flip would contradict the reason it is drawn where it is. **§6 shipped with it**, as the design required, but not in the shape the design assumed — and the difference was found by running it. The first build read `oauthAccount` out of `<configDir>/.claude.json`; running it showed the key is **not a reading of reachability**: a session printed a bare `Not logged in` in a directory whose `.claude.json` carried an `oauthAccount`. The key records *an account*; the credential behind it is a macOS keychain entry that can be locked, re-keyed or cleared without that file changing. So the check said *signed in* at the one moment the notice was wanted. Holi writes its own `.holi-spawned` marker into the config directory instead, and the notice fires on the **first spawn** in a vault. That is a proxy rather than a guess — credentials are keyed to the directory, so a directory Holi has never spawned in cannot be signed in — and it is the honest scope of the message, which is less "you are logged out" than "this vault is new, and that is why you are being asked again". **The same run corrected an overclaim**: what the migration carries is *files* — transcripts and the plugin set. The credential is not in the directory; it is a keychain entry Claude Code owns, so whether a login survives the rename is not Holi's to promise, and the notice is worded as "this vault is new" rather than "you are logged out" for exactly that reason.
| D87 — a vault's Google account is its own | Design of record: [`specs/2026-08-24-per-vault-google-design.md`](specs/2026-08-24-per-vault-google-design.md); agreed 2026-08-23 in [`specs/2026-08-23-per-vault-agent-silos-design.md`](specs/2026-08-23-per-vault-agent-silos-design.md) §Companion. **Built 2026-08-24.** Google tokens live in `userData/google-auth.enc` (`main/google/electron.ts:25`), machine-level by D67, so every vault on a machine shows the same Gmail and the same Calendar. Nobody saw it until a second vault existed, and what it looks like is a brand-new work vault showing personal mail. Settles that a vault connects its OWN account. A per-vault *toggle* over one shared account was the cheaper option and was rejected: the ask is genuinely "this vault connects to that account", not "hide the surfaces here". **The design halved the cost it was agreed under.** Two of the five listed obstacles were not obstacles: the token store is **already** `Record<sub, auth>`, a bet `token-store.ts` took deliberately ("the difference between multi-account is an additive change and multi-account is a storage migration"), so there is no storage migration and `GoogleSession.#current()` returning `Object.values()[0]` is the single line that assumes one account; and `holi-google` resolves no credentials at all, being a shell script that curls main, so it needs no change. What is real: one cache file per account (`google-cache-<sub>.db`), rejecting both composite keys — a schema migration to buy one file handle — and wipe-on-switch, which charges a full re-fetch on every vault switch, worst exactly where two vaults are used side by side. Settles that the vault→account map is `userData/google-vault-accounts.json` keyed by remote, machine-local and **outside** the vault, because a mapping inside the clone dies with it and every other decision about a connected account already lives in `userData`; that `GoogleSession` becomes one instance per `sub` rather than one taking a `sub` per call, since `#refreshing` is per-account state and a shared latch would serialise unrelated refreshes; and that disconnect is **two** actions — unlink this vault, or remove the account everywhere — rather than a refcount, which makes one button mean different things depending on invisible state. **Found while verifying D86 and folded in**: an agent session outlives a vault switch, so a backgrounded agent would read whatever vault is *active* — D87's own complaint arriving late and harder to see. The ops server therefore mints its bearer **per session, naming the vault**, which also expires a dead session's token. **No migration, deliberately**: nothing is mapped on upgrade, every vault asks on first use, and the reconnect is one click because the account is already in the store. D86's migration is the one part of that build that shipped wrong (it chose the vault by `lastOpenedAt`, which answers a different question, caught only by running it), and a second guess of the same kind is worth more than the click it saves — the payoff being that lazy connect becomes the **only** path, with no first-launch special case. Same shape as D86 (an identity Holi holds per machine that a vault should hold for itself) and should reuse its answers about where a credential lives and what a fresh vault does before it has one, but it is a separate build | **What the build added, both found by running it.** (a) The account picker stayed **empty until a reload**: the renderer asks once on mount, at startup that lands before the vault has finished opening, and `status`/`accounts` threw `no vault is open` — a transient failure the renderer recorded as an answer and never re-asked. Those two procedures now treat no-vault-open as *no vault's account* rather than a refusal, which is also the honest reading. (b) The shared account atom was reset on **every mount** to catch a vault switch, which broke "one query however many readers" — three components, three queries. It is guarded on the vault the held answer was fetched for instead. **Checked by hand against a real install**: the stale `google-cache.db` deleted on first launch, mail and agenda chips gone from a vault with no account, the connected account offered and linked in **one click with no browser**, a `google-cache-<sub>.db` appearing on first use, and — the one the design exists for — an agent started in `privat` still reading `privat`'s mail over its bearer after the active vault was switched to one with no account at all, then getting a **403 once its session was killed**.
| D85 — a vault says what it opens on, and is asked at birth | [`prd/daily-notes.md`](prd/daily-notes.md) FR-4 + §Timezone, [`prd/notes-editor.md`](prd/notes-editor.md) §Panes & tabs, [`prd/vaults-sync.md`](prd/vaults-sync.md) §`.holi/settings.json` has a schema. Agreed and built 2026-08-23 (worklist item 15). Settles that a **`landing` key** in the committed `.holi/settings.json`, under the per-key `.holi/settings.local.json` override the theme (D64) and icons (D82) already use, says what a vault opens on: today's daily, a note, a vault app, or the board, agenda or mail. **The daily is named by KIND, not by path**, because `{kind:'note',path:'23-08-2026.md'}` rots overnight and because the daily note is being rebuilt into a dashboard the target should keep pointing at. **One target, not a list**: `openSingleton` inserts leftmost and `openApp` appends, so a list whose order the strip contradicts is worse than no list; widening is additive and deferred, not foreclosed. **A target is a claim about the past**, so it is checked against the snapshot and a rotted one falls back by RE-RESOLVING (a missing note in a vault with `dailyNotes` off lands on nothing, not on a daily the vault said it did not want). **`dailyNotes` replaces `isPersonalVault`**, which inferred from the GitHub collaborator count whether you wanted a journal: a guess standing in for a question nobody had asked, paid for with a network round trip on every cold start and answered by a timeout when offline. Offline-complete stops being a fallback and becomes structural. The collision it guarded (two members, one `DD-MM-YYYY.md`) is unsolved and now *said out loud* at the moment of choosing; per-person paths are the answer if it bites, deferred to the dashboard rebuild. **Minting is not landing**, and shipping them fused was the bug this found in the running app: a vault pointed at its board minted no daily at all, which is a hole in the record found weeks later. `dailyNotes` decides whether the vault keeps a journal; `landing` only decides what you are looking at. The policy left `openTodaysDailyAtom`, which is what keeps ⌘⇧D working in a vault whose `dailyNotes` is off: off means *stop doing this behind my back*, not *the feature is gone*. **One descriptor list drives the onboarding step and the seed**, so the file a vault is born with and the questions it was asked cannot drift; adding a setting is adding a row. Each row carries `whereToChange` as **data**, so a row structurally cannot ship without saying where to change it later. The **join path does not ask**: an adopted repo's file already speaks. **Four rows, two files**: `dailyNotes`, `landing` and `hooks` are statements about the vault and are committed; `colorScheme` is a statement about you on this machine and is local, because a collaborator's committed choice flipping your app to light mode is the failure that layer exists to prevent. `maxCommittedFileBytes` deliberately gets no row, since freezing it into every vault means raising the default later never reaches the vaults that already exist. **`colorScheme` also ends the app being dark-only**: `index.css` has carried a full `[data-theme='light']` block since it was written and nothing ever stamped the attribute. Both modes are stamped explicitly (unstamped resolves dark by accident and leaves `color-scheme:` unset, which paints the native window background), `system` keeps a `matchMedia` listener so the app follows the OS *while running* rather than only at launch, and `useVaultTheme` takes the mode as a **dependency** because a resolved theme carries both blocks and a flip that did not re-apply would leave the other mode's overrides on the root. **The file got a schema on the way**: two hand-rolled readers became one `resolveVaultSettings` in `packages/shared`, and *data, never code* is now enforced by a whitelist rather than convention. A write crosses the same boundary a read does, which `fields` forced by accepting only strings and booleans and turned out to be the better design |
| D84 — a reorder is shown, not described | [`prd/notes-editor.md`](prd/notes-editor.md) §Moving a tab. Agreed and built 2026-08-22 (worklist item 10). Settles that dragging a pill **opens a hole**: the pills between its slot and the pointer slide aside, the pill you are holding dims to 40% (the tree's cut-row reading) and travels with them, and the caret line survives only for a tab arriving from **another** pane — that drag has no slot here to move out of and no width this strip could know. Gap for a reorder, caret for an insert. The offsets are `lib/tab-reorder.ts`, pure and on numbers, keyed by the same `to` index `dropIndex` hands the drop, so preview and drop cannot disagree. **Transforms, never layout**: nothing else moves, the strip is never wider mid-drag than at rest, and hit-testing switched to `offsetLeft` — a rect includes the preview's own transform, so testing against one moves the midpoints that decided the preview and the hole chases the pointer that opened it. **A drop clears the transform *and* its transition**, because the real positions become what the preview was showing and a transition left declared would animate the pill back from a move it never made; a drag that ends without a drop clears to **zeros** instead, keeping the transition so the pills glide home. Null and zeros being different states is the seam between the two endings. **Every other position change glides** (FLIP), measured in the **viewport** frame `offsetLeft - scrollLeft` and only when the tab list changed. Both halves were found in the running app: in content coordinates, closing a tab while scrolled flings every pill across a strip where nothing visibly moved (the scroll shortens with the content), and without the key-list gate a plain scroll animates the whole strip. Motion is read from `--duration-micro`/`--ease-settle` at use, since a WAAPI keyframe cannot carry a `var()`. **Amends D78**: the panes' landing strips no longer appear when a tab is picked up, but when the pointer **leaves the strip** — the commonest drag never leaves it, and two bands flashing under every nudge is noise. Reported by him within the hour of the preview landing. Nothing is learned later: leaving the strip is the earliest a drag can be heading for a pane |
| D83 — the tab strip scrolls, and counts what is off each edge | [`prd/notes-editor.md`](prd/notes-editor.md) §The strip scrolls, and says what is off each edge. Agreed and built 2026-08-22. **Replaces D78's clipping window and deletes `lib/tab-window.ts`.** The window rendered only the pills that fit and hid the rest behind one `+N`: correct clipping, wrong reading. A single count cannot say *which way* your tab went, nothing scrolled the strip at all, and "the window slides to keep the active tab visible" is a rule you must already know for the strip to make sense. Settles that every pill is laid out inside an `overflow-x-auto` viewport (`min-w-0` still load-bearing — a flex child that cannot shrink grows the pane instead of scrolling); that a **vertical wheel scrolls it sideways**, since a mouse without a horizontal wheel would otherwise have no gesture and a one-line row has nothing to scroll vertically; that the scrollbar is hidden, because a permanent bar in a 44px row eats a third of a pill and the side counts are the affordance instead; and that what is out of reach is counted **per side** — a chevron and a number at each end, each opening a menu of that side's tabs. **The per-side split is the whole point** and it came from the first framing being rejected: one count is a dead end the moment the strip can scroll in both directions. Three rules in `lib/tab-overflow.ts` (pure, tested on numbers, because jsdom computes no layout): a **partly visible pill counts as offscreen**, since half a filename answers neither "is there more" nor "how much"; an **unmeasured strip announces nothing**, or the count flashes on mount saying every tab is missing; and the **active tab is scrolled into view keyed on its identity**, not its index, so a reorder cannot yank the scroll back just as you dropped a different tab somewhere else. Picking from a menu selects **and** reveals, because the tab you scrolled away from may already be the active one and selecting it again moves nothing. The drag auto-slide became an auto-**scroll** on the same band: 12px every 16ms while the pointer sits within 28px of an end, which is what keeps an off-screen drop position expressible now that the wheel is unavailable mid-drag. **Amended within the hour, from his testing**: the counts sat in the flex row, so one emptying at the end of a scroll handed ~24px back to the viewport and re-laid every pill out mid-gesture, and unmounting it made it blink out on a frame — it read as a glitch. They now **float over the strip's edges and stay mounted**, fading over 160ms and showing their last non-empty number while they go (a blank pill fading away is the same glitch), `aria-hidden` and out of the tab order while invisible, with a `--background` gradient underneath so a pill scrolling beneath stays legible |
| D82 — an icon is a vault-level map, not a property of a file | [`prd/notes-editor.md`](prd/notes-editor.md) FR-13. Agreed and built the same day (2026-08-22), so it never sat in the inbox — and **reversed the same day**, which is the part worth reading. Settles that a row's icon is a single emoji in **`.holi/icons.json`**, a committed path → emoji map under a gitignored per-key `.holi/icons.local.json` override (the theme's layering, D64); that keys are normalized like every other vault path and written back sorted, so committing an icon does not churn the diff; that the value must be exactly one emoji and a bad one is ignored rather than truncated, since it lands in a fixed-size row; and that **"Edit Icon…" in the row menu** is the gesture, identical on a note, a folder and a binary. **What it reversed**: the first build put the icon in the note's own frontmatter, because that travels with the file and an agent's `mv` cannot detach it — a real advantage, and still the right instinct for a note. It was dropped because it can only serve things that *have* frontmatter: not a folder, not a PDF, not `CLAUDE.md`/`AGENTS.md` (read verbatim as the agent's instructions, so frontmatter there is prompt text), and not local-only markdown like `USER.local.md`, which the scan files outside `docs` where no `DocMeta` exists to hold a field. A map was needed for all of those anyway, and **two mechanisms with a precedence rule between them cost more to explain than the travelling property was worth**. The filename was rejected earlier and stays rejected: an emoji in the path makes a wiki-link writable only by someone who remembers the icon. **The rot is accepted, not answered** — a file moved outside Holi leaves a dead entry, which degrades to no icon rather than to lost content, and refusing to let anyone icon a folder to avoid a cosmetic staleness is the worse trade. **On what counts as an emoji**: `RGI_Emoji` holds exactly one spelling of each character — the bare form of an emoji-presentation one (⭐) and only the `…U+FE0F` form of a text-presentation one (❤️) — while the macOS picker emits the selector for both, so a single regex test silently refused what the palette produced; the value is also tried with its selectors stripped, and a lone `Extended_Pictographic` is accepted, which admits `❤`/`⚙` without admitting a word. **Where it shows**: the tree row and, since 2026-08-22, the note's **tab pill** — one file wears one icon wherever it appears. `tabIcon` is module-level and takes the map as an argument rather than reaching for a store of its own, which keeps the strip a projection of the snapshot like the tree |

**D1–D59 are spent, and D60 supersedes all of them.** They are not listed here any more, and that is deliberate: their subjects — the CRDT doc store, the file↔CRDT bridge, the task record and its file projection, the SSE event stream, server-side membership, snapshot history, the git mirror — do not exist. A ledger of decisions about a deleted system is archaeology pretending to be law, and the docs are law.

The reasoning is not lost. Every one of them was written up in full in this file and is recoverable from git history (`git log -p docs/decisions.md`); the ones whose *lessons* outlive their mechanism were carried into the rewritten docs as prose, unattributed to a number:

- **The task file is the whole task** — the projection's parser, serializer, and file grammar survive in `prd/tasks.md`, minus the record they reconciled against.
- **A store loaded by whoever renders it fails silently** (was D58) — `architecture.md` §9. The second consumer cannot tell an unloaded store from an empty one.
- **Machine tokens do not belong in a file a human edits** (was D33) — the same argument now keeps the reminder watermark out of the repo, because a write that fires on a timer becomes a commit.
- **Select by an explicit marker, never by filename shape** (was D46) — `prd/daily-notes.md`, and the reason `task.` is a filename prefix rather than a frontmatter key.
- **The client passes its local date; nothing else computes "today"** (was D44) — `prd/daily-notes.md`.
- **A rename must reject a colliding destination before moving anything** — `prd/notes-editor.md` FR-11.

**D59 was spent** on the offline main-as-sync-hub work (commits `f1584f6`…`9bc7eef`) without this ledger being updated. It is the clearest casualty of the pivot: four slices building a Yjs sync hub inside a machine that no longer has Yjs.

**D61 was spent** on making push automatic — amending D60 point 2's "explicit push" to a coalescing auto-push with a leave-point set, an optimistic non-fast-forward recovery, and an `offline — N waiting` / `no write access` failure taxonomy. Consolidated 2026-07-24 into `prd/vaults-sync.md` (§Summary, §Pushing, §State display, §Why these choices; the debounce and budget numbers it left open now sit in FR-4 and FR-13), `architecture.md` §Sync, and `glossary.md` (§Push replaces §Publish); the Publish button, `publish()`, `sync.publish`, and the `publishing`/`ahead` sync states are deleted from the code. Its reasoning is the §Why-these-choices "why push automatically" paragraph. **Fully realized in code 2026-07-25:** an audit found two survivors, now cleaned up — the agent seed (`agent/seed-content.ts` `AGENTS.md`) still told the agent commits "stay on this machine until the user presses **Publish**", now corrected to auto-push; and a dead duplicate `SyncState` union carrying the removed `ahead` kind lingered in `packages/shared/src/types.ts` (imported nowhere — the live union is in `main/vault/active-vault.ts`), now deleted.
